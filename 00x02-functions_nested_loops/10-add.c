#include "main.h"

/**
* add - adds two integers and returns the result.
*  @a , @b : integers
*  @r : result
*  Return : r , the result 
*/

int	add(int a, int b)
{
	int	r;

	r = a + b;
	return (r);
}
